// Mapea project.key (frontend) → projectId REAL de Carbonmark
export const PROJECT_LISTING_MAP: Record<string, string[]> = {
  'VCS-844': ['VCS-191'], // Madre de Dios
  // agregás más acá cuando haga falta
};
